# quick-task
keep task list up to date as a markdown with API and cli that agent can use. Support status, relationship (parent child, depends on), allows additional info such as long descriptions and doc links,  but managed as a markdown list that can be edited by human or agent
